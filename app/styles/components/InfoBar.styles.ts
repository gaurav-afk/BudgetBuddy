import { StyleSheet } from "react-native";
import { basicColors } from "../colors";
import typography from "../typography";

export default StyleSheet.create({
    snackbar: {
        // backgroundColor: basicColors.textPrimary
    }
});
